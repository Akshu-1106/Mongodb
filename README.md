# MongoDB Project

## 🚀About

This repository contains a **simple Node.js and MongoDB application**.

It includes an Express server and frontend HTML page that connects to a MongoDB database to perform basic operations.

---

## 📁 Repository Contents

- `server.js` – Main server file (Express + MongoDB)  
- `package.json` – Node.js project metadata  
- `package-lock.json` – Lock file for dependencies  
- `front.html` – Frontend HTML page  
- `README.md` – Project documentation  

---

## 📌 Requirements

To run this project, you need:

- **Node.js** (12+ recommended)  
- **MongoDB** (local or Atlas cluster)  
- Code editor (e.g., VS Code)

---

## 🛠️ Getting Started

Before running the project, make sure MongoDB is running:

If using local MongoDB:
1. Open a terminal
2. Start MongoDB service:
   ```bash
   mongod

If using MongoDB Atlas:
1. Get your connection string
2. Replace it in the server configuration

---

## 🛠️ How to Run
Option 1: Run Locally

1. Clone the repository:
   ```bash
   git clone https://github.com/Akshu-1106/Mongodb.git
3. Navigate into the project:
   ```bash
   cd Mongodb
5. Install dependencies:
    ```npm install```
6. Start the server:
   ```node server.js```
7. Open the frontend in your browser:
   ```front.html```

---

## 🧠 Usage
1. Once the server is running:
2. Use the frontend page (front.html) to interact with the application
3. Perform basic MongoDB operations (create, read, update, delete) through the UI

---

### Project Status
✅ Completed — Core MongoDB connection and basic functionality implemented.
